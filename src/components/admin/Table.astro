---
const tableHeaders = ['Producto', 'Precio', 'Categoría', 'Estado'];

const tableBody = [
  {
    product: 'Apple iMac 27',
    price: '1.47',
    category: 'Desktop PC',
    status: 'Active',
  },
  {
    product: 'Apple iMac 27',
    price: '1.47',
    category: 'Desktop PC',
    status: 'Active',
  },
];
---

<section class="flex flex-col items-start gap-4 w-full px-8">
  <button
    type="button"
    class="text-text hover:text-white border border-text hover:bg-text focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center"
    >Default</button
  >

  <!-- TABLA -->
  <div class="overflow-x-auto border border-text rounded-xl w-full">
    <table class="w-full text-sm text-left text-text-light">
      <!-- TABLA HEAD -->
      <thead class="text-xs text-text uppercase bg-gray-light">
        <tr>
          {
            tableHeaders.map((item) => (
              <th scope="col" class="px-4 py-3">
                {item}
              </th>
            ))
          }
        </tr>

        <!-- TABLA BODY -->
        <tbody>
          {
            tableBody.map((item) => (
              <tr class="border-b border-text hover:bg-gray-100">
                <th
                  scope="row"
                  class="px-4 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white"
                >
                  {item.product}
                </th>
                <td class="px-4 py-4">{item.price}</td>
                <td class="px-4 py-4">{item.category}</td>
                <td class="px-4 py-4">{item.status}</td>
              </tr>
            ))
          }
        </tbody>
      </thead>
    </table>
  </div>
</section>
